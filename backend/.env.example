# LongEntry Market Scanner — Environment Configuration
# Copy to .env and fill in your values.

# PostgreSQL connection string
LE_DATABASE_URL=postgresql://longentry:YOUR_PASSWORD_HERE@localhost:5432/longentry

# API key hash (SHA-256). Generate with:
#   python -c "import hashlib; print(hashlib.sha256(b'your-secret-key').hexdigest())"
LE_API_KEY_HASH=

# Logging
LE_LOG_LEVEL=INFO
LE_LOG_DIR=/var/log/longentry

# Ranking settings
LE_MAX_ACTIVE_MARKETS=6
LE_MAX_ACTIVE_STOCKS=4
LE_MIN_FINAL_SCORE=40.0

# Anthropic API key for AI-powered fundamental outlook (auto_outlook.py)
# Get one at: https://console.anthropic.com/settings/keys
LE_ANTHROPIC_API_KEY=
